import { Layout } from '@/components/layout/Layout';

export const ProductNewPage = () => {
  return (
    <Layout>
      <div className="max-w-content mx-auto px-20 py-8">
        <h1 className="text-3xl font-bold mb-6">글쓰기</h1>
        <p className="text-gray-600">글쓰기 폼이 여기에 표시됩니다.</p>
      </div>
    </Layout>
  );
};
